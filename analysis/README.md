# Analysis

Normalized area for actively maintained analysis code.

## Current status
- `analysis/scripts/` now contains the active migrated scripts.
- `analysis/notebooks/` is available for future notebook-based work.
- `2 - materiale/script/` is kept as a compatibility link.

## Migration rule
Move scripts here only after:
1. Replacing hardcoded paths with project-relative paths.
2. Declaring script inputs and outputs at the top of each file.
